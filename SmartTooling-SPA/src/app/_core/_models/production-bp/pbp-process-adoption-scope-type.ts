export interface PbpProcessAdoptionScopeType {
    factory_id: string;
    process_adoption_scope_id: string;
    process_adoption_scope_name: string;
    sequence: number;
    is_active: boolean;
    update_by: string;
    update_time: string;
}